# ── Core LangGraph / LangChain ────────────────────────────────────────────────
langgraph>=0.2
langchain>=0.2
langchain-groq>=0.1
langchain-core>=0.2

# ── LLM & Token Counting ──────────────────────────────────────────────────────
tiktoken>=0.7
groq>=0.9

# ── Vector DB (Tier 2 — Episodic RAG) ────────────────────────────────────────
qdrant-client>=1.9
sentence-transformers>=3.0

# ── Persistence ───────────────────────────────────────────────────────────────
langgraph-checkpoint-sqlite>=2.0   # SqliteSaver for cross-session checkpointing

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0
rich>=13.0        # Beautiful CLI output
pyfiglet>=1.0     # JARVIS ASCII art banner

# ── Voice Mode (required for --voice flag) ────────────────────────────────────
faster-whisper>=1.2.1  # Local STT: Whisper on CTranslate2 (tiny + small models)
edge-tts>=7.2.8        # TTS: Microsoft Neural Voices (en-GB-RyanNeural, hi-IN-MadhurNeural)
sounddevice>=0.5.5     # Microphone capture via PortAudio
playsound3>=3.3.1      # MP3 playback via Windows MCI (no compilation needed)
                       # NOTE: pygame NOT used — pygame 2.6.x fails on Python 3.14
psutil>=7.2.2          # CPU usage monitoring for wake word CPU guard
scipy>=1.11            # scipy.io.wavfile — save NumPy audio arrays as .wav for Whisper
transformers>=5.0      # Hinglish TTS: Aaryan39/hinglish-tts-3b-ft-synthetic
